# Medium 로그인에 사용할 이메일 주소
MEDIUM_EMAIL=test@test.com

# Gmail API 인증 정보 파일 경로 (기본값: credentials.json)
GMAIL_CREDENTIALS_PATH=credentials.json

# Gmail API 토큰 저장 경로 (자동 생성됨)
GMAIL_TOKEN_PATH=token.json

# Medium Sender email
SENDER_EMAIL=noreply@medium.com

# 크롤링할 URL 리스트 파일 경로
URLS_FILE=urls.txt

# 크롤링 결과 저장 디렉토리
OUTPUT_DIR=output

# DB Host, Port, UserId, Password
DB_HOST=
DB_PORT=
DB_USER=
DB_PASS=